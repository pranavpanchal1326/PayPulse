# Frontend environment. Copy to `.env.local` and adjust — Vite reads that file
# automatically and it is not committed.

# Where the API lives. Compose publishes it on 8000 and passes this through,
# so you only need this file when running `npm run dev` outside the stack.
VITE_API_BASE_URL=http://localhost:8000
